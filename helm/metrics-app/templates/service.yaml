apiVersion: v1
kind: Service
metadata:
  name: {{ include "metrics-app.fullname" . }}
spec:
  selector:
    app: {{ include "metrics-app.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080

